{
    "compilerOptions": {
      "baseUrl": ".", // 这是基本目录，对于别名解析到的根目录
      "paths": {
        "@/*": ["src/*"] // 这里假设 '@' 指向 'src' 目录
      }
    },
    "include": ["src/**/*"] // 这指定了 VS Code 应该包含在项目文件检索中的文件
  }